import { Argv } from "yargs";
export declare const command = "repo-config <command>";
export declare const desc = "Read or write Graphite's configuration settings for the current repo.";
export declare const builder: (yargs: Argv) => Argv;
