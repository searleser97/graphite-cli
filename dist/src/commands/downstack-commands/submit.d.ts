import { aliases, argsT, builder, command, description } from "../stack-commands/submit";
export { command, description, builder, aliases };
export declare const handler: (argv: argsT) => Promise<void>;
