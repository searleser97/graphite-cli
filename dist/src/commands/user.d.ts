import { Argv } from "yargs";
export declare const command = "user <command>";
export declare const desc = "Read or write Graphite's user configuration settings.";
export declare const builder: (yargs: Argv) => Argv;
